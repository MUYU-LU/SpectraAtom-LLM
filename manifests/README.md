# Manifests

This directory records external artifact locations and expected schemas.

Manifest files should point to data/checkpoints on compute11 or the cluster. They should not contain passwords or private keys.

