# Legacy Migration Notes

This directory is for migration notes only.

Do not copy large old projects here. Old project directories stay on compute11 and the 910 cluster. This repo should only contain:

- script mappings,
- run mappings,
- notes on what is deprecated,
- notes on what should be ported into clean scripts.

Use `legacy/script_mapping.md` before copying any old script.

